<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="crm_lead_view_form_inherit" model="ir.ui.view">
            <field name="name">crm.lead.view.form.inherit</field>
            <field name="model">crm.lead</field>
            <field name="inherit_id" ref="crm.crm_lead_view_form"/>
            <field name="arch" type="xml">
                <button name="action_set_won_rainbowman" position="attributes">
                    <attribute name="confirm">Are you sure to mark this lead as Won?</attribute>
                </button>
                <button name="%(crm.crm_lead_lost_action)d" position="attributes">
                    <attribute name="confirm">Are you sure to mark this lead as Lost?</attribute>
                </button>
            </field>
        </record>

    </data>
</odoo>